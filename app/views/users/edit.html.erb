<h1 class = "headings">Edit user</h1>
<h2><font color="green"><%= flash[:notice] %></font></h2>

<%= render 'form' %>

<%= if current_user.role 
      link_to 'Back', admin_home_path, :class => "middle" 
    else 
	  link_to 'Back', :back, :class => "middle" 
    end 
%>