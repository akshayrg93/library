<h1 class = "headings">Add new book</h1>
<% if @book.errors.any? %>
  <div class="error_explanation">
    <li><%= @book.errors.full_messages.first if @book.errors.any? %></li>
  </div>
<% end %>
<%= render 'form' %>

<br><%= link_to 'Back', books_path, :class => "middle" %><br>